{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node start.cjs",
    "build": "vite build && cp -r dist build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
